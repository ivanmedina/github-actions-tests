name: labeler

on: [ pull_request ]

jobs:
  labeler: 
    runs-on: ubuntu-latest
    name: Label the PR
    steps:
      - name: Pull request size labeler
        uses: ivanmedina/pr-size-labeler@v1.0.2
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
